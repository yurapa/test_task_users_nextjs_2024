import React from 'react';
import Nav from '@/components/Nav';

export default function ForgotPage() {
  return (
    <div>
      <Nav />
      <main className="text-center">
        <h2>Forgot password?</h2>
        <p>It's a pity, but we can't help you with that.</p>
      </main>
    </div>
  );
}
